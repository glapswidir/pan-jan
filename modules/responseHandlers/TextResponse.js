class TextResponse {
    handleResponse(response) {
        // Logic to handle text response
        console.log('Text Response:', response);
        // Display the response as text or log it
    }
}

module.exports = TextResponse;
